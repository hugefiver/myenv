# DisplayLink / 多 GPU 兼容性变量。
# 固定让 Hyprland 优先用 Intel iGPU 渲染，同时允许 evdi(DisplayLink) 输出。

export AQ_DRM_DEVICES=/dev/dri/intel-igpu:/dev/dri/displaylink-card

# DisplayLink + 多 GPU 下常见的小窗口损坏 / 同步异常缓解项。
export AQ_MGPU_NO_EXPLICIT=1

# USB 显示链路下的鼠标卡顿、光标异常，优先先禁用硬件光标平面。
export WLR_NO_HARDWARE_CURSORS=1